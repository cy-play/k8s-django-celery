## Configure django with celery and run everything in Kubernetes

This projects shows an example of configuration django with celery.

Everything runs inside Kubernetes cluster on Docker containers.

